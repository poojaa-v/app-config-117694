management:
  security:
    enabled: false
endpoints:
    web:
      exposure:
        include: /actuator/refresh
actuator:
     enabled: true
logging:
  level:
    io:
      online: INFO
spring:
  h2:
    console:
      settings:
        web-allow-others: true
