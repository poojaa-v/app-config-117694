security:
  basic:
    enabled: false
management:
  security:
    enabled: false
endpoints:
    web:
      exposure:
        include: /actuator/refresh
actuator:
     enabled: true
logging:
  level:
    io:
      online: DEBUG
